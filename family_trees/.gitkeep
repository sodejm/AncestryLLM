# This file keeps the otherwise-empty family_trees/ directory under version
# control. Place your private RootsMagic .rmtree files here locally — they are
# ignored by .gitignore and must never be committed to a public repository.
